import 'package:flutter/material.dart';

class ProfileDevicelog extends StatefulWidget {
  const ProfileDevicelog({ Key? key }) : super(key: key);

  @override
  State<ProfileDevicelog> createState() => _ProfileDevicelogState();
}

class _ProfileDevicelogState extends State<ProfileDevicelog> {
  @override
  Widget build(BuildContext context) {
    return Container(
       child: Center(
        child: Text('life token'),
      ),
    );
  }
}